apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: devboard
  namespace: devboard
spec:
  hosts:
    - "*"
  gateways:
    - devboard-gateway
  http:
    # All traffic goes to api-gateway which internally routes to microservices
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: api-gateway
            port:
              number: 8080
      timeout: 30s
      retries:
        attempts: 3
        perTryTimeout: 10s
        retryOn: gateway-error,connect-failure,retriable-4xx
