apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: devboard
spec:
  # Enforce mTLS for all services in the devboard namespace
  mtls:
    mode: STRICT
