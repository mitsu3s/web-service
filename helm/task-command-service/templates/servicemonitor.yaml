{{- if .Values.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "task-command-service.fullname" . }}
  labels:
    {{- include "task-command-service.labels" . | nindent 4 }}
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      {{- include "task-command-service.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http-envoy-prom
      path: /stats/prometheus
      interval: {{ .Values.serviceMonitor.interval }}
{{- end }}
