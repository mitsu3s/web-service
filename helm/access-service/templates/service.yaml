apiVersion: v1
kind: Service
metadata:
  name: {{ include "access-service.fullname" . }}
  labels:
    {{- include "access-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "access-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
