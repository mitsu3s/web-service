{{- if .Values.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "board-service.fullname" . }}
  labels:
    {{- include "board-service.labels" . | nindent 4 }}
    # This label is required by kube-prometheus-stack to auto-discover monitors
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      {{- include "board-service.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http-envoy-prom
      path: /stats/prometheus
      interval: {{ .Values.serviceMonitor.interval }}
{{- end }}
