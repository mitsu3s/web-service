apiVersion: v1
kind: Service
metadata:
  name: {{ include "activity-service.fullname" . }}
  labels:
    {{- include "activity-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "activity-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
