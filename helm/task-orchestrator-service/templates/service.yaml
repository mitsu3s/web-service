apiVersion: v1
kind: Service
metadata:
  name: {{ include "task-orchestrator-service.fullname" . }}
  labels:
    {{- include "task-orchestrator-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "task-orchestrator-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
